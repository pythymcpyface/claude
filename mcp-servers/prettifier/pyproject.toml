[project]
name = "prettifier-mcp"
version = "1.0.0"
description = "MCP server for automatic rich text formatting"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]

[project.optional-dependencies]
rich = ["rich>=13.0.0"]
yaml = ["pyyaml>=6.0"]
