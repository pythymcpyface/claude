# Aliases

Map short commands to their full equivalents to save typing and tokens.

- `/fix-db` -> `/migrate-schema`
- `/opt-algo` -> `/consolidate-algorithm`
- `/qa` -> `/quality-check`
- `/git` -> `/git-process`
- `/e2e` -> `/generate-e2e-tests`
